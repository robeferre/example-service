jobs:
  build:
    docker:
      - image: alpine:latest
    steps:
      - checkout
      - run: echo "Building..."
  test:
    docker:
      - image: alpine:latest
    steps:
      - checkout
      - run: echo "Testing..."
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
